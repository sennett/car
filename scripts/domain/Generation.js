define(['../../bower_components/underscore/underscore'], function(_) {
    var Generation = function(){};
    
    Generation.prototype = _.extend(Generation.prototype, {});
    
    return Generation;
});